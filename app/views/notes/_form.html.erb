<%= form_for(note) do |f| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title, class: "sr-only" %>
    <%= f.text_field :title, class: "form_control", placeholder: "Title here" %>
  </div>

  <div class="field">
    <%= f.label :body, class: "sr-only" %>
    <%= f.text_area :body, class: "form-control", placeholder: "Body here", rows: 10 %>
  </div>

  <div class="actions">
    <%= f.submit (@note.persisted? ? "Update Note" : "Save Note"), class: "btn btn-warning btn-sm" %>
  </div>
<% end %>
